import { style } from '@vanilla-extract/css';

export const wrapper = style({
  display: `flex`,
  gap: `16px`,
});

export const image = style({
  position: `relative`,
  width: `100%`,
  marginBottom: `16px`,
});
