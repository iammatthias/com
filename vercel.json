{
  "framework": "astro",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "bun run build",
        "outputDirectory": ".vercel/output"
      }
    }
  ]
}
