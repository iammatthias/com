---
const response = await fetch("https://glass.photo/api/v2/users/iam/posts?limit=25");
const data = await response.json();
const posts = data.map((post) => {
  const { id, description, created_at: published_at, image1024x1024: cover_image } = post;
  return { id, description, published_at, cover_image };
});
---

{posts}
